# feedback.yaml — сюда попадают твои оценки из Google Таблицы (через
# `python sheets_sync.py pull`). Используется как few-shot-примеры для ИИ.
# Руками обычно не редактируется — но можно добавить пару стартовых примеров,
# чтобы система не была "холодной" в первый день.

examples: []
